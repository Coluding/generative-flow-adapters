name: flow_output_shortcut
model:
  type: flow
  provider: dummy
  feature_dim: 32
  hidden_dim: 128
  freeze: true
adapter:
  type: output
  hidden_dim: 128
conditioning:
  type: action
  input_dim: 8
  output_dim: 64
  include_horizon: true
training:
  loss: flow
  learning_rate: 0.0001
  local_consistency_weight: 0.5
  multistep_consistency_weight: 0.25
